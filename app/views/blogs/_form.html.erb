<%= simple_form_for(@blog) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :headingone %>
    <%= f.input :bodyone %>
    <%= f.input :headingtwo %>
    <%= f.input :bodytwo %>
    <%= f.input :headingthree %>
    <%= f.input :bodythree %>
    <%= f.input :published_at %>
    <%= f.input :slug %>
    <%= f.input :image_filename,
                label: "Image filename (e.g. blog-post-header.jpg)",
                hint: "File must already exist in app/assets/images/" %>
  </div>

  <% if f.object.image_filename.present? %>
    <div class="mb-3">
      <p class="mb-1">Current image:</p>
      <%= image_tag f.object.image_filename, class: "img-thumbnail", style: "max-height: 150px;" %>
    </div>
  <% end %>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>